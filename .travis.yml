dist: trusty
language: php
php:
  - 5.5
  - 5.6
  - 7.0

env:
  - TEST_SUITE=meqp1

before_script:
  - sleep 60

script:
  - wget https://github.com/magento/marketplace-eqp/archive/1.0.2.zip
  - unzip -qq 1.0.2.zip
  - wget https://github.com/squizlabs/PHP_CodeSniffer/releases/download/2.6.2/phpcs.phar && chmod +x phpcs.phar
  - sh -c "if [ '$TEST_SUITE' = 'meqp1' ]; then ./phpcs.phar --config-set installed_paths ./marketplace-eqp-1.0.2 fi"
  - ./phpcs.phar -n --standard="MEQP1" ./magemonkey

notifications:
  slack: ebizmarts:k7A0FCTvGxkJdxsDITHlJG0Z
